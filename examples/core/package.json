{
  "name": "example-core",
  "private": true,
  "version": "0.0.0",
  "description": "Vanilla TypeScript example app for the Viostream player core SDK",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@viostream/viostream-player-core": "file:../../packages/viostream-player-core"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
